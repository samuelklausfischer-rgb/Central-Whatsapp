<style>
/* ===== Slide 01 — Capa ===== */
.s1-content {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  padding: 58px 72px 46px;
}
/* ---- Logo com halo/glow radial (sem placa) — print-safe ---- */
.s1-logo {
  position: relative;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}
.s1-logo__halo {
  position: absolute;
  left: 50%; top: 50%;
  width: 980px; height: 460px;
  transform: translate(-50%, -50%);
  pointer-events: none;
  background:
    radial-gradient(288px 120px at 50% 49%,
      rgba(255,255,255,0.88) 0%,
      rgba(255,255,255,0.84) 32%,
      rgba(250,251,255,0.48) 48%,
      rgba(235,237,250,0.19) 64%,
      rgba(235,237,250,0.00) 83%),
    radial-gradient(510px 205px at 50% 51%,
      rgba(214,220,244,0.19) 0%,
      rgba(214,220,244,0.06) 47%,
      rgba(214,220,244,0.00) 75%);
  filter: blur(11px);
}
.s1-logo img { position: relative; height: 80px; width: auto; display: block; }
.s1-hero {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  padding-bottom: 12px;
}
.s1-kicker {
  display: flex;
  align-items: center;
  gap: 26px;
  color: var(--lavanda);
  font-size: 15px;
  font-weight: var(--fw-semibold);
  letter-spacing: var(--ls-caps-wide);
  text-transform: uppercase;
  white-space: nowrap;
}
.s1-kicker::before,
.s1-kicker::after {
  content: "";
  width: 70px;
  height: 1.5px;
  background: rgba(255, 255, 255, 0.32);
}
.s1-title {
  margin-top: 34px;
  font-size: 56px;
  font-weight: var(--fw-semibold);
  line-height: 1.22;
  letter-spacing: var(--ls-caps-tight);
  color: var(--branco);
  max-width: 1120px;
}
.s1-dest {
  margin-top: 36px;
  font-size: 21px;
  font-weight: var(--fw-regular);
  color: var(--lavanda);
  letter-spacing: 0.4px;
}
.s1-foot {
  width: 780px;
  padding-top: 20px;
  border-top: 1px solid rgba(255, 255, 255, 0.20);
  font-size: 15px;
  letter-spacing: 0.8px;
  color: var(--branco-70);
  font-weight: var(--fw-regular);
}
</style>
<section class="page gradient-capa" id="page-1">
  <div class="bleed">
    <div class="blob blob--branco" style="width:680px;height:580px;top:-240px;right:-190px;opacity:0.07;"></div>
    <div class="blob blob--branco blob--alt" style="width:590px;height:520px;bottom:-250px;left:-200px;opacity:0.06;"></div>
    <div class="blob blob--branco blob--alt" style="width:240px;height:210px;top:170px;left:110px;opacity:0.045;"></div>
    <div class="blob blob--branco" style="width:190px;height:170px;bottom:150px;right:190px;opacity:0.045;"></div>
  </div>
  <div class="s1-content">
    <div class="s1-logo"><span class="s1-logo__halo"></span><img src="assets/logo.png" alt="PRN Diagnósticos"></div>
    <div class="s1-hero">
      <div class="s1-kicker">PROPOSTA COMERCIAL</div>
      <h1 class="s1-title">TELERRADIOLOGIA E GESTÃO<br>DE DIAGNÓSTICO POR IMAGEM</h1>
      <p class="s1-dest">Preparada para {{ cliente.artigo }} {{ cliente.nome }} — {{ cliente.cidade_uf }}</p>
    </div>
    <div class="s1-foot">{{ proposta.cidade_emissao }}, {{ proposta.data }} &middot; Validade da proposta: {{ proposta.validade|nbsp }}</div>
  </div>
</section>

<style>
/* ===== Slide 02 — Quem Somos ===== */
.s2-grid {
  position: absolute;
  inset: 0;
  padding: 64px 64px 84px;
  display: grid;
  grid-template-columns: 1fr 596px;
  gap: 56px;
  align-items: center;
}
.s2-left .slide-title { font-size: 44px; }
.s2-para {
  margin-top: 30px;
  font-size: 19px;
  line-height: 1.7;
  color: var(--navy);
  max-width: 600px;
}
.s2-cards {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
}
.s2-card {
  background: var(--grad-navy);
  color: var(--branco);
  border-radius: var(--radius-card);
  box-shadow: var(--shadow-deep);
  padding: 26px 26px 24px;
}
.s2-card h3 {
  margin-top: 18px;
  font-size: 18px;
  font-weight: var(--fw-semibold);
  line-height: 1.32;
}
.s2-card p {
  margin-top: 8px;
  font-size: 17px;
  line-height: 1.5;
  color: var(--branco-85);
}
.s2-card .icon-circle { width: 52px; height: 52px; }
.s2-card .icon-circle svg { width: 25px; height: 25px; }
</style>
<section class="page page-lavanda" id="page-2">
  <div class="bleed">
    <div class="blob blob--indigo" style="width:520px;height:460px;bottom:-210px;right:-170px;opacity:0.05;"></div>
  </div>
  <div class="s2-grid">
    <div class="s2-left">
      <h2 class="slide-title slide-title--accent">QUASE DUAS DÉCADAS<br>DE DIAGNÓSTICO<br>POR IMAGEM</h2>
      <p class="s2-para">Fundada em 2007, a PRN Diagnósticos é referência em telerradiologia e gestão de diagnóstico por imagem. Nossos laudos são emitidos exclusivamente por médicos especialistas em cada área. Operamos em nuvem, com RIS/PACS e suporte 24&nbsp;horas. Atendemos hospitais e clínicas em 19&nbsp;estados do país.</p>
    </div>
    <div class="s2-cards">
      <div class="s2-card">
        <div class="icon-circle icon-circle--invert">
          <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><circle cx="12" cy="12" r="9.5"/><polyline points="12 6.5 12 12 15.5 14"/></svg>
        </div>
        <h3>Suporte operacional 24h</h3>
        <p>Equipe disponível todos os dias, inclusive feriados e fins de semana.</p>
      </div>
      <div class="s2-card">
        <div class="icon-circle icon-circle--invert">
          <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M4 13a8 8 0 0 1 16 0"/><path d="M4 13h2a1.5 1.5 0 0 1 1.5 1.5v3A1.5 1.5 0 0 1 6 19H5.5A1.5 1.5 0 0 1 4 17.5z"/><path d="M20 13h-2a1.5 1.5 0 0 0-1.5 1.5v3A1.5 1.5 0 0 0 18 19h.5a1.5 1.5 0 0 0 1.5-1.5z"/><path d="M20 17.5v1a3 3 0 0 1-3 3h-3.5"/></svg>
        </div>
        <h3>Atendimento remoto humanizado</h3>
        <p>Pessoas reais no atendimento a médicos e equipes assistenciais.</p>
      </div>
      <div class="s2-card">
        <div class="icon-circle icon-circle--invert">
          <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><rect x="9" y="3" width="6" height="5" rx="1.2"/><rect x="3" y="16" width="6" height="5" rx="1.2"/><rect x="15" y="16" width="6" height="5" rx="1.2"/><path d="M12 8v4"/><path d="M12 12H6v4"/><path d="M12 12h6v4"/></svg>
        </div>
        <h3>Gestão completa do setor</h3>
        <p>Do laudo isolado à operação integral do diagnóstico por imagem.</p>
      </div>
      <div class="s2-card">
        <div class="icon-circle icon-circle--invert">
          <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M18 10.5h-1.26A7 7 0 1 0 9 19.5h9a4.5 4.5 0 0 0 0-9z"/></svg>
        </div>
        <h3>Tecnologia em nuvem (RIS/PACS)</h3>
        <p>Imagens e laudos seguros, acessíveis de qualquer lugar.</p>
      </div>
    </div>
  </div>
  <footer class="footer-slide">
    <span class="footer-brand"><span class="footer-mark">PRN</span> PRN Diagn&oacute;sticos</span>
    <span>{{ cliente.nome }} &middot; {{ cliente.cidade_uf }}</span>
    <span class="footer-num">02 / 13</span>
  </footer>
</section>

<style>
/* ===== Slide 03 — PRN em Números ===== */
.s3-wrap {
  position: absolute;
  inset: 0;
  padding: 70px 64px 96px;
  display: flex;
  flex-direction: column;
  align-items: center;
}
.s3-title {
  font-size: 46px;
  font-weight: var(--fw-bold);
  line-height: var(--lh-title);
  letter-spacing: var(--ls-caps);
  color: var(--branco);
  text-align: center;
}
.s3-numbers {
  flex: 1;
  width: 100%;
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  align-items: center;
}
.s3-cell {
  height: 296px;
  padding: 0 18px;
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}
.s3-cell + .s3-cell { border-left: 1px solid rgba(255, 255, 255, 0.15); }
.s3-tick {
  width: 34px;
  height: 3px;
  border-radius: 2px;
  background: rgba(232, 233, 245, 0.45);
  margin: 0 auto;
}
.s3-value {
  height: 138px;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  align-items: center;
}
.s3-num {
  font-size: 72px;
  font-weight: var(--fw-extrabold);
  line-height: 1;
  letter-spacing: -1.5px;
  color: var(--branco);
  font-variant-numeric: tabular-nums;
  white-space: nowrap;
}
.s3-pre {
  display: block;
  font-size: 20px;
  font-weight: var(--fw-semibold);
  letter-spacing: var(--ls-caps);
  text-transform: uppercase;
  color: var(--lavanda);
  margin-bottom: 14px;
}
.s3-rotulo {
  margin-top: 18px;
  font-size: 17px;
  font-weight: var(--fw-medium);
  color: var(--lavanda);
}
.s3-chips-block { width: 100%; text-align: center; }
.s3-chips-label {
  font-size: var(--fs-label);
  font-weight: var(--fw-semibold);
  letter-spacing: var(--ls-caps);
  text-transform: uppercase;
  color: var(--branco-70);
}
.s3-chips {
  margin-top: 18px;
  display: flex;
  justify-content: center;
  gap: 14px;
  white-space: nowrap;
}
.s3-chip {
  display: inline-flex;
  align-items: center;
  border: 1px solid var(--lavanda-40);
  border-radius: var(--radius-pill);
  padding: 10px 22px;
  font-size: 14px;
  font-weight: var(--fw-medium);
  letter-spacing: 1.8px;
  text-transform: uppercase;
  color: var(--lavanda);
  white-space: nowrap;
}
</style>
<section class="page page-escuro" id="page-3">
  <div class="bleed">
    <div class="blob" style="width:560px;height:480px;top:-220px;right:-170px;opacity:0.08;"></div>
    <div class="blob blob--alt" style="width:480px;height:420px;bottom:-210px;left:-160px;opacity:0.08;"></div>
  </div>
  <div class="s3-wrap">
    <h2 class="s3-title">A PRN EM NÚMEROS</h2>
    <div class="s3-numbers">
      <div class="s3-cell">
        <div class="s3-tick"></div>
        <div class="s3-value"><div class="s3-num">19</div></div>
        <div class="s3-rotulo">estados atendidos</div>
      </div>
      <div class="s3-cell">
        <div class="s3-tick"></div>
        <div class="s3-value"><div class="s3-num">+82.028</div></div>
        <div class="s3-rotulo">exames laudados por mês</div>
      </div>
      <div class="s3-cell">
        <div class="s3-tick"></div>
        <div class="s3-value"><div class="s3-num">+781.937</div></div>
        <div class="s3-rotulo">exames laudados por ano</div>
      </div>
      <div class="s3-cell">
        <div class="s3-tick"></div>
        <div class="s3-value"><div class="s3-num"><span class="s3-pre">Desde</span>2007</div></div>
        <div class="s3-rotulo">em operação contínua</div>
      </div>
    </div>
    <div class="s3-chips-block">
      <div class="s3-chips-label">Modalidades laudadas</div>
      <div class="s3-chips">
        <span class="s3-chip">Ressonância Magnética</span>
        <span class="s3-chip">Tomografia</span>
        <span class="s3-chip">Mamografia</span>
        <span class="s3-chip">Raio-X</span>
        <span class="s3-chip">Densitometria Óssea</span>
        <span class="s3-chip">Ultrassom</span>
      </div>
    </div>
  </div>
  <footer class="footer-slide">
    <span class="footer-brand"><span class="footer-mark">PRN</span> PRN Diagn&oacute;sticos</span>
    <span>{{ cliente.nome }} &middot; {{ cliente.cidade_uf }}</span>
    <span class="footer-num">03 / 13</span>
  </footer>
</section>

<style>
/* ===== Slide 04 — Entendimento da necessidade ===== */
.s4-head { display: flex; align-items: flex-start; gap: 28px; margin-bottom: 44px; }
.s4-hicon { margin-top: 10px; }
.s4-title {
  max-width: 730px;
  font-size: 44px;
  letter-spacing: 4px;
  color: var(--indigo-escuro);
}
.s4-ponte {
  flex: 1;
  min-width: 0;
  align-self: stretch;
  display: flex;
  align-items: center;
  margin-top: 4px;
  padding-left: 36px;
  border-left: 1.5px solid var(--indigo-10);
  font-size: 17.5px;
  line-height: 1.62;
  color: var(--navy);
}
.s4-objeto {
  background: var(--lavanda);
  border-left: 6px solid var(--bordo);
  border-radius: var(--radius-card);
  padding: 32px 40px;
}
.s4-objeto-txt {
  margin-top: 10px;
  font-size: 19px;
  line-height: 1.62;
  color: var(--navy);
  max-width: 1190px;
}
.s4-vol-label { display: block; margin: 46px 0 18px; }
.s4-minis { gap: 24px; }
.s4-mini {
  background: var(--branco);
  border: 1.5px solid var(--indigo-10);
  border-radius: 16px;
  box-shadow: var(--shadow-soft);
  padding: 24px 26px;
}
.s4-sigla {
  display: inline-flex;
  padding: 5px 15px;
  border-radius: var(--radius-pill);
  background: var(--bordo);
  color: var(--branco);
  font-size: 12.5px;
  font-weight: var(--fw-bold);
  letter-spacing: 3px;
}
.s4-mini-val {
  margin-top: 18px;
  font-size: 46px;
  font-weight: var(--fw-extrabold);
  line-height: 1;
  color: var(--indigo);
  font-variant-numeric: tabular-nums;
}
.s4-mini-rot {
  margin-top: 7px;
  font-size: 15px;
  font-weight: var(--fw-medium);
  color: var(--ink-muted);
}
</style>
<section class="page page-pad" id="page-4">
  <div class="bleed">
    <div class="blob blob--indigo blob--soft" style="width:480px;height:420px;bottom:-180px;right:-160px;"></div>
  </div>

  <div class="s4-head">
    <div class="icon-circle s4-hicon">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M16 4h2a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h2"/><rect x="8" y="2" width="8" height="4" rx="1"/><path d="M9 12h6"/><path d="M9 16h4"/></svg>
    </div>
    <h1 class="slide-title s4-title">O QUE {{ cliente.artigo|upper }} {{ cliente.nome|upper }} PRECISA HOJE</h1>
    <p class="s4-ponte">O hospital precisa de laudos rápidos e confiáveis, 24 horas por dia. E precisa disso sem ampliar a estrutura interna nem contratar novos especialistas. É exatamente o que esta proposta entrega.</p>
  </div>

  <div class="s4-objeto">
    <p class="label-caps">Objeto da proposta</p>
    <p class="s4-objeto-txt">Serviços de telerradiologia com sistema PACS online, acesso nos consultórios médicos e entrega online dos resultados. Imagens e laudos armazenados em nuvem. Laudos realizados exclusivamente por médicos especialistas, 24 horas por dia, 7 dias por semana, com suporte humanizado.</p>
  </div>

  <span class="label-caps s4-vol-label">Volumetria mensal informada</span>
  <div class="s4-minis" style="display:grid; grid-template-columns: repeat({{ exames|length }}, 1fr); gap:24px;">
    {% for e in exames %}
    <div class="s4-mini">
      <span class="s4-sigla">{{ e.sigla }}</span>
      <div class="s4-mini-val">{{ e.volume }}</div>
      <div class="s4-mini-rot">{{ e.unidade }}/mês</div>
    </div>
    {% endfor %}
  </div>

  <footer class="footer-slide">
    <span class="footer-brand"><span class="footer-mark">PRN</span> PRN Diagnósticos</span>
    <span>{{ cliente.nome }} · {{ cliente.cidade_uf }}</span>
    <span class="footer-num">04 / 13</span>
  </footer>
</section>

<style>
/* ===== Slide 05 — Como funciona na prática ===== */
.s5-title {
  color: var(--indigo-escuro);
  letter-spacing: 4px;
  margin-bottom: 52px;
}
.s5-flow {
  position: relative;
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 24px;
}
.s5-track {
  position: absolute;
  top: 31px;
  left: 155px;
  right: 155px;
  border-top: 2.5px dashed rgba(21, 65, 101, 0.45);
}
.s5-col { display: flex; flex-direction: column; }
.s5-num {
  position: relative;
  width: 64px;
  height: 64px;
  margin: 0 auto 22px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--branco);
  font-size: 21px;
  font-weight: var(--fw-bold);
  border: 4px solid var(--lavanda);
  box-shadow: var(--shadow-soft);
}
.s5-num--navy  { background: var(--grad-navy); }
.s5-num--bordo { background: var(--grad-bordo); }
.s5-card {
  flex: 1;
  background: var(--branco);
  border-radius: var(--radius-card);
  box-shadow: var(--shadow-card);
  padding: 34px 26px 30px;
  text-align: center;
}
.s5-card .icon-circle { margin: 0 auto 20px; background: var(--lavanda); }
.s5-step-title {
  font-size: 18px;
  font-weight: var(--fw-semibold);
  line-height: 1.3;
  color: var(--indigo);
  min-height: 47px;
  display: flex;
  align-items: center;
  justify-content: center;
}
.s5-step-text {
  margin-top: 10px;
  font-size: 16px;
  line-height: 1.55;
  color: var(--ink-soft);
}
.s5-faixa {
  margin-top: 44px;
  background: linear-gradient(135deg, #950d1c 0%, #171658 100%);
  color: var(--branco-85);
  border-radius: 16px;
  box-shadow: var(--shadow-deep);
  padding: 25px 48px;
  text-align: center;
  font-size: 19.5px;
  line-height: 1.5;
}
.s5-faixa strong { font-weight: var(--fw-semibold); color: var(--branco); }
</style>
<section class="page page-pad page-lavanda" id="page-5">
  <div class="bleed">
    <div class="blob blob--indigo blob--soft" style="width:460px;height:400px;top:-170px;right:-150px;"></div>
    <div class="blob blob--navy blob--soft blob--alt" style="width:400px;height:360px;bottom:-160px;left:-140px;"></div>
  </div>

  <h1 class="slide-title slide-title--accent s5-title">COMO FUNCIONA NA PRÁTICA</h1>

  <div class="s5-flow">
    <div class="s5-track"></div>
    <div class="s5-col">
      <div class="s5-num s5-num--navy">01</div>
      <div class="s5-card">
        <div class="icon-circle icon-circle--lg">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 4v16"/><path d="M2 8h18a2 2 0 0 1 2 2v10"/><path d="M2 17h20"/><path d="M6 8v9"/></svg>
        </div>
        <h3 class="s5-step-title">Exame realizado no hospital</h3>
        <p class="s5-step-text">Sua equipe realiza o exame normalmente, sem mudar a rotina.</p>
      </div>
    </div>
    <div class="s5-col">
      <div class="s5-num s5-num--bordo">02</div>
      <div class="s5-card">
        <div class="icon-circle icon-circle--lg">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 14.9A7 7 0 1 1 15.7 8h1.8a4.5 4.5 0 0 1 2.5 8.2"/><path d="M12 12v9"/><path d="m16 16-4-4-4 4"/></svg>
        </div>
        <h3 class="s5-step-title">Envio automático</h3>
        <p class="s5-step-text">As imagens seguem via PACS em nuvem, sem intervenção manual.</p>
      </div>
    </div>
    <div class="s5-col">
      <div class="s5-num s5-num--navy">03</div>
      <div class="s5-card">
        <div class="icon-circle icon-circle--lg">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7z"/><path d="M14 2v4a2 2 0 0 0 2 2h4"/><path d="m9 15 2 2 4-4"/></svg>
        </div>
        <h3 class="s5-step-title">Laudo por especialista</h3>
        <p class="s5-step-text">Um médico da subespecialidade correspondente analisa e lauda o exame.</p>
      </div>
    </div>
    <div class="s5-col">
      <div class="s5-num s5-num--bordo">04</div>
      <div class="s5-card">
        <div class="icon-circle icon-circle--lg">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M8 21h8"/><path d="M12 17v4"/><path d="m9 9.5 2 2 4.5-4.5"/></svg>
        </div>
        <h3 class="s5-step-title">Resultado online</h3>
        <p class="s5-step-text">Médicos e pacientes acessam laudo e imagens de onde estiverem.</p>
      </div>
    </div>
  </div>

  <div class="s5-faixa"><strong>Central de Laudos do Grupo PRN</strong> — 24 horas, 7 dias por semana, com suporte humanizado.</div>

  <footer class="footer-slide">
    <span class="footer-brand"><span class="footer-mark">PRN</span> PRN Diagnósticos</span>
    <span>{{ cliente.nome }} · {{ cliente.cidade_uf }}</span>
    <span class="footer-num">05 / 13</span>
  </footer>
</section>

<style>
/* ===== Slide 06 — Prazos com compromisso ===== */
.s6-wrap {
  position: relative;
  height: 100%;
  display: flex;
  flex-direction: column;
}
.s6-title {
  letter-spacing: var(--ls-caps-tight);
  margin-bottom: 52px;
  flex: none;
}
.s6-cards {
  flex: none;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 28px;
}
.s6-card {
  border-radius: var(--radius-lg);
  padding: 64px 36px 56px;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}
.s6-card--destaque {
  background: var(--grad-bordo);
  color: var(--branco);
  box-shadow: var(--shadow-deep);
}
.s6-card--outline {
  background: var(--branco);
  border: 1.5px solid var(--indigo-10);
  box-shadow: var(--shadow-soft);
}
.s6-value {
  margin-top: 30px;
  display: flex;
  align-items: baseline;
  justify-content: center;
  gap: 10px;
}
.s6-prefix, .s6-suffix {
  font-size: 21px;
  font-weight: var(--fw-medium);
  color: var(--branco-85);
}
.s6-card--outline .s6-prefix,
.s6-card--outline .s6-suffix { color: var(--ink-muted); }
.s6-num {
  font-size: 72px;
  font-weight: var(--fw-extrabold);
  line-height: 1;
  letter-spacing: -2px;
  color: var(--branco);
  font-variant-numeric: tabular-nums;
}
.s6-card--outline .s6-num { color: var(--indigo); }
.s6-rotulo {
  margin-top: 22px;
  font-size: 18px;
  font-weight: var(--fw-medium);
  line-height: 1.42;
  color: var(--branco-85);
  max-width: 250px;
}
.s6-card--outline .s6-rotulo { color: var(--ink-soft); }
.s6-card .icon-circle { width: 84px; height: 84px; }
.s6-card .icon-circle svg { width: 38px; height: 38px; }
.s6-nota {
  margin-top: auto;
  background: var(--lavanda);
  border-radius: 14px;
  padding: 22px 32px;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 14px;
}
.s6-nota svg {
  width: 22px;
  height: 22px;
  flex: none;
  stroke: var(--navy);
  stroke-width: 2;
  fill: none;
  stroke-linecap: round;
  stroke-linejoin: round;
}
.s6-nota p {
  font-size: 16px;
  line-height: 1.4;
  color: var(--navy);
}
</style>
<section class="page page-pad" id="page-6">
  <div class="bleed">
    <div class="blob blob--indigo blob--soft" style="width:460px;height:400px;bottom:-180px;right:-150px;"></div>
  </div>

  <div class="s6-wrap">
  <h1 class="slide-title slide-title--accent s6-title">PRAZOS COM COMPROMISSO</h1>

  <div class="s6-cards">
    <div class="s6-card s6-card--destaque">
      <div class="icon-circle icon-circle--lg icon-circle--invert">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M13 2 3 14h7l-1 8 10-12h-7l1-8z"/></svg>
      </div>
      <div class="s6-value"><span class="s6-prefix">até</span><span class="s6-num">2</span><span class="s6-suffix">horas</span></div>
      <p class="s6-rotulo">Urgências e pronto atendimento</p>
    </div>

    <div class="s6-card s6-card--outline">
      <div class="icon-circle icon-circle--lg">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="6" rx="1.5"/><rect x="5" y="8" width="5" height="4" rx="1.2"/><path d="M3 17v3"/><path d="M21 17v3"/></svg>
      </div>
      <div class="s6-value"><span class="s6-prefix">até</span><span class="s6-num">24</span><span class="s6-suffix">horas</span></div>
      <p class="s6-rotulo">Pacientes internados e preferenciais</p>
    </div>

    <div class="s6-card s6-card--outline">
      <div class="icon-circle icon-circle--lg">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
      </div>
      <div class="s6-value"><span class="s6-prefix">até</span><span class="s6-num">2</span><span class="s6-suffix">dias&nbsp;úteis</span></div>
      <p class="s6-rotulo">Exames eletivos e ambulatório</p>
    </div>
  </div>

  <div class="s6-nota">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="m8 12.5 2.5 2.5 5-5"/></svg>
    <p>Antecipação de laudos sob demanda. Prazos de urgência valem também em feriados e fins de semana.</p>
  </div>
  </div>

  <footer class="footer-slide">
    <span class="footer-brand"><span class="footer-mark">PRN</span> PRN Diagnósticos</span>
    <span>{{ cliente.nome }} · {{ cliente.cidade_uf }}</span>
    <span class="footer-num">06 / 13</span>
  </footer>
</section>

<style>
/* ===== Slide 07 — Implantação em 4 etapas ===== */
.s7-page { background: var(--indigo); color: var(--branco); }
.s7-wrap {
  position: relative;
  height: 100%;
  padding: 72px 64px 100px;
  display: flex;
  flex-direction: column;
}
.s7-title {
  font-size: 46px;
  font-weight: var(--fw-bold);
  line-height: 1.15;
  letter-spacing: var(--ls-caps);
  text-transform: uppercase;
  color: var(--branco);
  flex: none;
}
.s7-timeline {
  flex: 1;
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 24px;
  align-content: center;
  margin-top: 10px;
}
.s7-col {
  position: relative;
  text-align: center;
  padding: 0 4px;
}
.s7-node {
  position: relative;
  width: 18px;
  height: 18px;
  margin: 0 auto;
  border-radius: 50%;
  background: var(--bordo);
  box-shadow: 0 0 0 6px rgba(149, 13, 28, 0.28);
}
.s7-col::before {
  content: "";
  position: absolute;
  top: 8px;
  left: calc(50% + 22px);
  right: calc(-50% + 22px);
  height: 2px;
  background: rgba(255, 255, 255, 0.2);
}
.s7-col:last-child::before { display: none; }
.s7-ghost {
  margin-top: 6px;
  font-size: 190px;
  font-weight: var(--fw-extrabold);
  line-height: 0.82;
  letter-spacing: -6px;
  color: rgba(232, 233, 245, 0.3);
  font-variant-numeric: tabular-nums;
}
.s7-body { margin-top: -8px; }
.s7-step-title {
  font-size: 21px;
  font-weight: var(--fw-semibold);
  color: var(--branco);
  letter-spacing: 0.3px;
}
.s7-step-text {
  margin-top: 14px;
  font-size: 16px;
  line-height: 1.6;
  color: var(--lavanda);
}
.s7-page .footer-slide { color: var(--branco-70); }
</style>
<section class="page s7-page" id="page-7">
  <div class="bleed">
    <div class="blob" style="width:500px;height:440px;top:-180px;right:-150px;"></div>
  </div>

  <div class="s7-wrap">
    <h1 class="s7-title">IMPLANTAÇÃO EM 4 ETAPAS</h1>

    <div class="s7-timeline">
      <div class="s7-col">
        <div class="s7-node"></div>
        <div class="s7-ghost">01</div>
        <div class="s7-body">
          <h3 class="s7-step-title">Pesquisa</h3>
          <p class="s7-step-text">Levantamento de volumes e modalidades. Diagnóstico técnico DICOM. Definição de SLAs e KPIs.</p>
        </div>
      </div>
      <div class="s7-col">
        <div class="s7-node"></div>
        <div class="s7-ghost">02</div>
        <div class="s7-body">
          <h3 class="s7-step-title">Desenvolvimento</h3>
          <p class="s7-step-text">Configuração de PACS/RIS e integrações HL7/DICOM. Templates de laudo. Adequação à LGPD.</p>
        </div>
      </div>
      <div class="s7-col">
        <div class="s7-node"></div>
        <div class="s7-ghost">03</div>
        <div class="s7-body">
          <h3 class="s7-step-title">Operação assistida</h3>
          <p class="s7-step-text">Início dos laudos com monitoramento diário. Suporte 24/7 para as equipes do hospital.</p>
        </div>
      </div>
      <div class="s7-col">
        <div class="s7-node"></div>
        <div class="s7-ghost">04</div>
        <div class="s7-body">
          <h3 class="s7-step-title">Entrega final</h3>
          <p class="s7-step-text">Portal de exames em pleno uso. Relatórios mensais e QBR. Melhoria contínua de processos.</p>
        </div>
      </div>
    </div>
  </div>

  <footer class="footer-slide">
    <span class="footer-brand"><span class="footer-mark">PRN</span> PRN Diagnósticos</span>
    <span>{{ cliente.nome }} · {{ cliente.cidade_uf }}</span>
    <span class="footer-num">07 / 13</span>
  </footer>
</section>

<style>
/* ===== SLIDE 8 — Investimento mensal (utilitário) ===== */
.s8-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: var(--sp-4);
}
.s8-sub {
  margin-top: 16px;
  max-width: 960px;
}
.s8-badge {
  display: inline-flex;
  align-items: center;
  gap: 12px;
  margin-top: 10px;
  padding: 13px 26px;
  border: 1.5px solid var(--bordo);
  border-radius: var(--radius-pill);
  background: var(--branco);
  color: var(--bordo);
  font-size: var(--fs-label);
  font-weight: var(--fw-semibold);
  letter-spacing: var(--ls-caps-tight);
  text-transform: uppercase;
  white-space: nowrap;
}
.s8-badge svg {
  width: 18px;
  height: 18px;
  flex: none;
  stroke: currentColor;
  stroke-width: 2;
  fill: none;
  stroke-linecap: round;
  stroke-linejoin: round;
}
.s8-table { margin-top: 36px; }
.s8-table thead th {
  padding: 20px 40px;
  font-size: 14px;
}
.s8-table tbody td {
  padding: 23px 40px;
  font-size: 20px;
}
.s8-table td.s8-mod {
  font-weight: var(--fw-medium);
  color: var(--ink);
}
.s8-table th.s8-mid,
.s8-table td.s8-mid { text-align: center; }
.s8-table td.s8-mid {
  color: var(--ink-soft);
  font-variant-numeric: tabular-nums;
}
.s8-table td.num {
  color: var(--bordo);
  font-size: 21px;
}
.s8-notes {
  margin-top: 30px;
  padding-top: 22px;
  border-top: 1.5px solid var(--indigo-10);
  display: flex;
  flex-direction: column;
  gap: 12px;
}
.s8-note {
  display: flex;
  align-items: center;
  gap: 14px;
  font-size: 17px;
  line-height: var(--lh-tight);
  color: var(--ink-soft);
}
.s8-note svg {
  width: 21px;
  height: 21px;
  flex: none;
  stroke: var(--bordo);
  stroke-width: 2;
  fill: none;
  stroke-linecap: round;
  stroke-linejoin: round;
}
</style>

<section class="page" id="page-8">
  <div class="bleed">
    <div class="blob blob--indigo" style="width: 400px; height: 330px; right: -210px; bottom: -220px; opacity: 0.05;"></div>
  </div>

  <div class="page-pad">
    <div class="s8-head">
      <div>
        <h2 class="slide-title slide-title--accent">Investimento mensal</h2>
        <p class="subtitle s8-sub">Valores unit&aacute;rios por laudo, com base na volumetria informada pelo hospital.</p>
      </div>
      <div class="s8-badge">
        <svg viewBox="0 0 24 24" aria-hidden="true">
          <rect x="3" y="5" width="18" height="16" rx="2"/>
          <path d="M16 3v4M8 3v4M3 11h18"/>
        </svg>
        Validade: {{ proposta.validade|nbsp }}
      </div>
    </div>

    <table class="table-invest s8-table">
      <colgroup>
        <col style="width: 44%;">
        <col style="width: 28%;">
        <col style="width: 28%;">
      </colgroup>
      <thead>
        <tr>
          <th>Modalidade</th>
          <th class="s8-mid">Volume mensal estimado</th>
          <th class="num">Valor unit&aacute;rio</th>
        </tr>
      </thead>
      <tbody>
        {% for e in exames %}
        <tr>
          <td class="s8-mod">{{ e.nome }}</td>
          <td class="s8-mid">{{ e.volume }} {{ e.unidade }}</td>
          <td class="num">R$&nbsp;{{ e.valor }}</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>

    <div class="s8-notes">
      <div class="s8-note">
        <svg viewBox="0 0 24 24" aria-hidden="true">
          <path d="M12 3l7 3v5c0 4.6-3 8.7-7 10-4-1.3-7-5.4-7-10V6l7-3z"/>
          <path d="M9 12l2 2 4-4"/>
        </svg>
        <span>Valores incluem todas as despesas: transporte, manuten&ccedil;&atilde;o, encargos sociais e trabalhistas e impostos.</span>
      </div>
      <div class="s8-note">
        <svg viewBox="0 0 24 24" aria-hidden="true">
          <path d="M14 3H7a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V8l-5-5z"/>
          <path d="M14 3v5h5"/>
          <path d="M9 13h6M9 17h4"/>
        </svg>
        <span>Faturamento conforme produ&ccedil;&atilde;o efetiva. Proposta v&aacute;lida por {{ proposta.validade|nbsp }}.</span>
      </div>
    </div>
  </div>

  <footer class="footer-slide">
    <div class="footer-brand">
      <div class="footer-mark">PRN</div>
      <span>PRN Diagn&oacute;sticos</span>
    </div>
    <span>{{ cliente.nome }} &middot; {{ cliente.cidade_uf }}</span>
    <div class="footer-num">08 / 13</div>
  </footer>
</section>

<style>
/* ===== SLIDE 9 — Condições gerais ===== */
.s9-cols {
  display: grid;
  grid-template-columns: 1fr 552px;
  gap: var(--sp-4);
  align-items: stretch;
  margin-top: 44px;
  height: 540px;
}
.s9-ficha {
  background: var(--branco);
  border-radius: var(--radius-card);
  box-shadow: var(--shadow-card);
  padding: 42px 46px;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 30px 40px;
  align-content: center;
}
.s9-item { min-width: 0; }
.s9-item--full { grid-column: 1 / -1; }
.s9-rotulo {
  font-size: 11.5px;
  font-weight: var(--fw-semibold);
  letter-spacing: 2.5px;
  text-transform: uppercase;
  color: var(--bordo);
  margin-bottom: 7px;
}
.s9-valor {
  font-size: 17px;
  font-weight: var(--fw-medium);
  line-height: 1.42;
  color: var(--navy);
}
.s9-valor .s9-strong { font-weight: var(--fw-semibold); }
.s9-valor .s9-sec {
  font-weight: var(--fw-regular);
  color: var(--ink-muted);
}
.s9-decl {
  background: var(--branco);
  border-left: 4px solid var(--bordo);
  border-radius: var(--radius-card);
  box-shadow: var(--shadow-card);
  padding: 44px 46px;
  display: flex;
  flex-direction: column;
  justify-content: center;
}
.s9-decl-head {
  display: flex;
  align-items: center;
  gap: 16px;
  margin-bottom: 24px;
}
.s9-decl-head .icon-circle {
  width: 48px;
  height: 48px;
}
.s9-decl-head .icon-circle svg {
  width: 24px;
  height: 24px;
}
.s9-decl p {
  font-size: 17px;
  line-height: 1.62;
  color: var(--navy);
}
.s9-decl p + p { margin-top: 16px; }
</style>

<section class="page page-lavanda" id="page-9">
  <div class="bleed">
    <div class="blob blob--indigo blob--soft" style="width: 520px; height: 430px; right: -180px; bottom: -200px;"></div>
    <div class="blob blob--branco" style="width: 420px; height: 360px; left: -170px; top: -160px; opacity: 0.45;"></div>
  </div>

  <div class="page-pad">
    <h2 class="slide-title slide-title--accent">Condi&ccedil;&otilde;es gerais</h2>

    <div class="s9-cols">
      <div class="s9-ficha">
        <div class="s9-item">
          <div class="s9-rotulo">Empresa</div>
          <div class="s9-valor">PRN Servi&ccedil;os de Radiologia Ltda</div>
        </div>
        <div class="s9-item">
          <div class="s9-rotulo">CNPJ</div>
          <div class="s9-valor">08.646.447/0001-44</div>
        </div>
        <div class="s9-item s9-item--full">
          <div class="s9-rotulo">Representante legal</div>
          <div class="s9-valor">Paulo Rogerio Novack &middot; CPF 161.137.538-08</div>
        </div>
        <div class="s9-item">
          <div class="s9-rotulo">Validade da proposta</div>
          <div class="s9-valor">{{ proposta.validade|nbsp }}</div>
        </div>
        <div class="s9-item">
          <div class="s9-rotulo">Data</div>
          <div class="s9-valor">{{ proposta.cidade_emissao }}, {{ proposta.data }}</div>
        </div>
        <div class="s9-item s9-item--full">
          <div class="s9-rotulo">Telefones</div>
          <div class="s9-valor">(55)&nbsp;99112-0419 (WhatsApp) &middot; (47)&nbsp;99137-8313</div>
        </div>
        <div class="s9-item s9-item--full">
          <div class="s9-rotulo">E-mail</div>
          <div class="s9-valor"><span class="s9-strong">contato@prndiagnosticos.com.br</span> <span class="s9-sec">&middot; patihuth@hotmail.com</span></div>
        </div>
      </div>

      <div class="s9-decl">
        <div class="s9-decl-head">
          <div class="icon-circle">
            <svg viewBox="0 0 24 24" aria-hidden="true">
              <path d="m16 16 3-8 3 8c-.87.65-1.92 1-3 1s-2.13-.35-3-1Z"/>
              <path d="m2 16 3-8 3 8c-.87.65-1.92 1-3 1s-2.13-.35-3-1Z"/>
              <path d="M7 21h10"/>
              <path d="M12 3v18"/>
              <path d="M3 7h2c2 0 5-1 7-2 2 1 5 2 7 2h2"/>
            </svg>
          </div>
          <span class="label-caps">Declara&ccedil;&otilde;es</span>
        </div>
        <p>Declaramos que o pre&ccedil;o ofertado inclui todas as despesas do objeto: transporte, manuten&ccedil;&atilde;o preventiva e corretiva, encargos sociais, impostos, taxas, tributos, contribui&ccedil;&otilde;es, seguros e demais despesas inerentes.</p>
        <p>Declaramos inteira submiss&atilde;o aos preceitos legais, em especial &agrave; Lei&nbsp;n&ordm;&nbsp;10.520/2002 e &agrave; Lei&nbsp;n&ordm;&nbsp;8.666/1993.</p>
      </div>
    </div>
  </div>

  <footer class="footer-slide">
    <div class="footer-brand">
      <div class="footer-mark">PRN</div>
      <span>PRN Diagn&oacute;sticos</span>
    </div>
    <span>{{ cliente.nome }} &middot; {{ cliente.cidade_uf }}</span>
    <div class="footer-num">09 / 13</div>
  </footer>
</section>

<style>
/* ===== SLIDE 10 — ALÉM DOS LAUDOS ===== */
.s10-band {
  position: relative;
  height: 190px;
  background: linear-gradient(135deg, #7e0005 0%, #171658 100%);
  padding: 0 64px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  color: var(--branco);
}
.s10-band-bleed { position: absolute; inset: 0; overflow: hidden; pointer-events: none; }
.s10-band .blob { background: var(--branco); }
.s10-band-inner { position: relative; }
.s10-title {
  font-size: 34px;
  font-weight: var(--fw-bold);
  line-height: 1.15;
  letter-spacing: 4px;
  text-transform: uppercase;
  color: var(--branco);
}
.s10-lead {
  margin-top: 13px;
  font-size: 19px;
  line-height: 1.5;
  color: var(--lavanda);
  max-width: 1120px;
}
.s10-grid {
  position: absolute;
  left: 64px; right: 64px;
  top: 218px; bottom: 68px;
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  grid-template-rows: 1fr 1fr;
  gap: 18px;
}
.s10-card {
  background: var(--branco);
  border: 1.5px solid var(--indigo-10);
  border-radius: 16px;
  box-shadow: var(--shadow-soft);
  padding: 20px 22px;
  display: flex;
  flex-direction: column;
  min-width: 0;
}
.s10-card-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 13px;
}
.s10-ico {
  width: 40px; height: 40px;
  border-radius: 12px;
  background: var(--bordo-08);
  color: var(--bordo);
  display: flex; align-items: center; justify-content: center;
  flex: none;
}
.s10-ico svg {
  width: 20px; height: 20px;
  stroke: currentColor; stroke-width: 2; fill: none;
  stroke-linecap: round; stroke-linejoin: round;
}
.s10-num {
  font-size: 18px;
  font-weight: var(--fw-bold);
  color: var(--bordo);
  opacity: 0.45;
  letter-spacing: 1px;
  font-variant-numeric: tabular-nums;
}
.s10-card h3 {
  font-size: 16.5px;
  font-weight: var(--fw-semibold);
  color: var(--navy);
  line-height: 1.3;
  margin-bottom: 7px;
}
.s10-card p {
  font-size: 14.5px;
  line-height: 1.48;
  color: var(--ink-muted);
}
</style>
<section class="page" id="page-10">
  <div class="s10-band">
    <div class="s10-band-bleed">
      <div class="blob" style="width: 460px; height: 380px; right: -120px; top: -140px; opacity: 0.08;"></div>
      <div class="blob blob--alt" style="width: 300px; height: 240px; left: 420px; bottom: -170px; opacity: 0.05;"></div>
    </div>
    <div class="s10-band-inner">
      <h2 class="s10-title">Além dos laudos: gestão completa do setor</h2>
      <p class="s10-lead">Podemos assumir a gestão completa ou parcial do seu setor de imagem. Um único parceiro, do exame ao resultado, ponta a ponta.</p>
    </div>
  </div>

  <div class="s10-grid">
    <article class="s10-card">
      <div class="s10-card-head">
        <span class="s10-ico"><svg viewBox="0 0 24 24"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg></span>
        <span class="s10-num">01</span>
      </div>
      <h3>Equipe técnica especializada</h3>
      <p>Supervisão de radiologista sênior: protocolos otimizados e máxima produtividade das máquinas, sobretudo a RM.</p>
    </article>

    <article class="s10-card">
      <div class="s10-card-head">
        <span class="s10-ico"><svg viewBox="0 0 24 24"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/><polyline points="3.27 6.96 12 12.01 20.73 6.96"/><line x1="12" y1="22.08" x2="12" y2="12"/></svg></span>
        <span class="s10-num">02</span>
      </div>
      <h3>Fornecimento de insumos</h3>
      <p>Contratos nacionais de aquisição garantem insumos com custo menor para o hospital.</p>
    </article>

    <article class="s10-card">
      <div class="s10-card-head">
        <span class="s10-ico"><svg viewBox="0 0 24 24"><polyline points="6 9 6 2 18 2 18 9"/><path d="M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2"/><rect x="6" y="14" width="12" height="8" rx="1"/></svg></span>
        <span class="s10-num">03</span>
      </div>
      <h3>Impressão supervisionada</h3>
      <p>Exames impressos sob supervisão de médico especialista, com qualidade de imagem assegurada.</p>
    </article>

    <article class="s10-card">
      <div class="s10-card-head">
        <span class="s10-ico"><svg viewBox="0 0 24 24"><ellipse cx="12" cy="5" rx="9" ry="3"/><path d="M21 12c0 1.66-4 3-9 3s-9-1.34-9-3"/><path d="M3 5v14c0 1.66 4 3 9 3s9-1.34 9-3V5"/></svg></span>
        <span class="s10-num">04</span>
      </div>
      <h3>Gestão de PACS e RIS</h3>
      <p>Escopo pronto para operar um centro de diagnóstico completo e eficiente.</p>
    </article>

    <article class="s10-card">
      <div class="s10-card-head">
        <span class="s10-ico"><svg viewBox="0 0 24 24"><path d="M9 5H7a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V7a2 2 0 0 0-2-2h-2"/><rect x="9" y="3" width="6" height="4" rx="1"/><polyline points="9 13.5 11 15.5 15 11.5"/></svg></span>
        <span class="s10-num">05</span>
      </div>
      <h3>Equipe administrativa treinada</h3>
      <p>Mão de obra administrativa capacitada para a rotina do setor de imagem.</p>
    </article>

    <article class="s10-card">
      <div class="s10-card-head">
        <span class="s10-ico"><svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><line x1="12" y1="8" x2="12" y2="16"/><line x1="8" y1="12" x2="16" y2="12"/></svg></span>
        <span class="s10-num">06</span>
      </div>
      <h3>Médicos especialistas</h3>
      <p>Corpo clínico com especialistas em cada área do diagnóstico por imagem.</p>
    </article>

    <article class="s10-card">
      <div class="s10-card-head">
        <span class="s10-ico"><svg viewBox="0 0 24 24"><path d="m3 11 18-5v12L3 14v-3z"/><path d="M11.6 16.8a3 3 0 1 1-5.8-1.6"/></svg></span>
        <span class="s10-num">07</span>
      </div>
      <h3>Marketing e parcerias</h3>
      <p>Divulgação, agendamento e parcerias comerciais para elevar a produtividade do setor.</p>
    </article>

    <article class="s10-card">
      <div class="s10-card-head">
        <span class="s10-ico"><svg viewBox="0 0 24 24"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></svg></span>
        <span class="s10-num">08</span>
      </div>
      <h3>Manutenção e engenharia clínica</h3>
      <p>Manutenção preventiva e corretiva com equipe especializada e gestão de engenharia clínica.</p>
    </article>
  </div>

  <footer class="footer-slide">
    <div class="footer-brand"><span class="footer-mark">PRN</span> PRN Diagnósticos</div>
    <span>{{ cliente.nome }} · {{ cliente.cidade_uf }}</span>
    <span class="footer-num">10 / 13</span>
  </footer>
</section>

<style>
/* ===== SLIDE 11 — RESULTADOS REAIS ===== */
.s11-wrap {
  position: relative;
  height: 100%;
  padding: 66px 64px 96px;
  display: flex;
  flex-direction: column;
}
.s11-kicker {
  font-size: var(--fs-label);
  font-weight: var(--fw-semibold);
  letter-spacing: var(--ls-caps-wide);
  text-transform: uppercase;
  color: var(--branco-70);
  margin-bottom: 14px;
}
.s11-title {
  font-size: 34px;
  font-weight: var(--fw-bold);
  line-height: 1.18;
  letter-spacing: 3px;
  text-transform: uppercase;
  color: var(--branco);
}
.s11-cases {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 28px;
  margin-top: 40px;
}
.s11-case {
  background: var(--indigo);
  border-radius: var(--radius-card);
  border-top: 4px solid var(--bordo);
  box-shadow: var(--shadow-deep);
  padding: 34px 40px 32px;
}
.s11-chip {
  display: inline-flex;
  align-items: center;
  gap: 9px;
  padding: 8px 20px;
  border-radius: var(--radius-pill);
  background: var(--lavanda);
  color: var(--indigo);
  font-size: var(--fs-label);
  font-weight: var(--fw-semibold);
  letter-spacing: var(--ls-caps-tight);
  text-transform: uppercase;
  white-space: nowrap;
}
.s11-chip svg {
  width: 15px; height: 15px; flex: none;
  stroke: currentColor; stroke-width: 2; fill: none;
  stroke-linecap: round; stroke-linejoin: round;
}
.s11-value {
  margin-top: 24px;
  font-size: 80px;
  font-weight: var(--fw-extrabold);
  line-height: 1;
  letter-spacing: -1.5px;
  color: var(--branco);
  font-variant-numeric: tabular-nums;
  white-space: nowrap;
}
.s11-unit {
  display: block;
  margin-top: 10px;
  font-size: 21px;
  font-weight: var(--fw-semibold);
  letter-spacing: 0.5px;
  color: var(--lavanda);
}
.s11-case p {
  margin-top: 16px;
  font-size: 17px;
  line-height: 1.55;
  color: var(--branco-85);
}
.s11-quote {
  margin: auto auto 0;
  max-width: 1000px;
  position: relative;
  text-align: center;
  font-size: 22px;
  font-weight: var(--fw-light);
  line-height: 1.6;
  color: var(--lavanda);
  padding-top: 34px;
}
.s11-quote::before {
  content: "\201C";
  position: absolute;
  top: -6px;
  left: 50%;
  transform: translateX(-50%);
  font-size: 84px;
  line-height: 1;
  font-weight: var(--fw-bold);
  color: var(--branco);
  opacity: 0.22;
}
</style>
<section class="page page-escuro" id="page-11">
  <div class="bleed">
    <div class="blob" style="width: 620px; height: 520px; right: -180px; top: -220px; opacity: 0.08;"></div>
    <div class="blob blob--alt" style="width: 480px; height: 420px; left: -200px; bottom: -230px; opacity: 0.06;"></div>
  </div>

  <div class="s11-wrap">
    <div class="s11-kicker">Resultados reais</div>
    <h2 class="s11-title">O que acontece quando a PRN assume a operação</h2>

    <div class="s11-cases">
      {% for c in cases %}
      <article class="s11-case">
        <span class="s11-chip"><svg viewBox="0 0 24 24"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>{{ c.cidade_uf }}</span>
        <div class="s11-value">{{ c.numero }}<span class="s11-unit">{{ c.unidade }}</span></div>
        <p>{{ c.descricao }}</p>
      </article>
      {% endfor %}
    </div>

    <p class="s11-quote">A capacidade instalada já existe no hospital. A gestão especializada é o que transforma essa capacidade em resultado.</p>
  </div>

  <footer class="footer-slide">
    <div class="footer-brand"><span class="footer-mark">PRN</span> PRN Diagnósticos</div>
    <span>{{ cliente.nome }} · {{ cliente.cidade_uf }}</span>
    <span class="footer-num">11 / 13</span>
  </footer>
</section>

<style>
/* ===== SLIDE 12 — PRÓXIMOS PASSOS + CONTATO ===== */
.s12-wrap {
  position: relative;
  height: 100%;
  padding: 70px 64px 116px;
  display: flex;
  flex-direction: column;
}
.s12-title {
  font-size: 58px;
  font-weight: var(--fw-bold);
  line-height: 1.08;
  letter-spacing: 4px;
  text-transform: uppercase;
  color: var(--branco);
}
.s12-cols {
  display: grid;
  grid-template-columns: 1.32fr 1fr;
  gap: 48px;
  align-items: center;
  flex: 1;
  margin-top: 30px;
}
.s12-steps {
  display: flex;
  flex-direction: column;
  gap: 26px;
}
.s12-step {
  position: relative;
  display: flex;
  align-items: center;
  gap: 20px;
  background: var(--branco-10);
  border: 1px solid rgba(255, 255, 255, 0.18);
  border-radius: var(--radius-pill);
  padding: 19px 34px 19px 20px;
}
.s12-step::after {
  content: "";
  position: absolute;
  left: 44px;
  top: 100%;
  height: 26px;
  width: 2px;
  background: rgba(255, 255, 255, 0.22);
}
.s12-step:last-child::after { display: none; }
.s12-step-num {
  width: 48px; height: 48px;
  border-radius: 50%;
  flex: none;
  display: flex; align-items: center; justify-content: center;
  background: var(--branco-16);
  border: 1px solid rgba(255, 255, 255, 0.28);
  color: var(--branco);
  font-size: 20px;
  font-weight: var(--fw-bold);
}
.s12-step h3 {
  font-size: 18.5px;
  font-weight: var(--fw-semibold);
  color: var(--branco);
  line-height: 1.3;
  margin-bottom: 3px;
}
.s12-step p {
  font-size: 15.5px;
  line-height: 1.45;
  color: var(--lavanda);
}
.s12-contact {
  background: var(--branco-10);
  border: 1px solid rgba(255, 255, 255, 0.22);
  border-radius: var(--radius-lg);
  padding: 30px 34px;
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
}
.s12-contact-label {
  font-size: var(--fs-label);
  font-weight: var(--fw-semibold);
  letter-spacing: var(--ls-caps);
  text-transform: uppercase;
  color: var(--branco-85);
  margin-bottom: 20px;
}
.s12-contact-row {
  display: flex;
  align-items: center;
  gap: 16px;
  padding: 9px 0;
}
.s12-contact-ico {
  width: 38px; height: 38px;
  border-radius: 50%;
  flex: none;
  display: flex; align-items: center; justify-content: center;
  background: var(--branco-16);
  color: var(--branco);
}
.s12-contact-ico svg {
  width: 18px; height: 18px;
  stroke: currentColor; stroke-width: 2; fill: none;
  stroke-linecap: round; stroke-linejoin: round;
}
.s12-contact-row span {
  font-size: 17px;
  color: var(--lavanda);
  min-width: 0;
}
.s12-contact-row.s12-destaque span {
  font-size: 18px;
  font-weight: var(--fw-semibold);
  color: var(--branco);
}
.s12-sign {
  position: absolute;
  left: 64px; right: 64px;
  bottom: 42px;
  border-top: 1px solid rgba(255, 255, 255, 0.2);
  padding-top: 18px;
  font-size: 16px;
  color: var(--lavanda);
}
.s12-sign strong {
  font-weight: var(--fw-semibold);
  color: var(--branco);
}
</style>
<section class="page gradient-capa" id="page-12">
  <div class="bleed">
    <div class="blob blob--branco" style="width: 640px; height: 540px; right: -200px; top: -240px; opacity: 0.08;"></div>
    <div class="blob blob--branco blob--alt" style="width: 500px; height: 430px; left: -220px; bottom: -240px; opacity: 0.06;"></div>
  </div>

  <div class="s12-wrap">
    <h2 class="s12-title">Vamos começar?</h2>

    <div class="s12-cols">
      <div class="s12-steps">
        <div class="s12-step">
          <span class="s12-step-num">1</span>
          <div>
            <h3>Aceite e alinhamento de escopo</h3>
            <p>Confirmação da proposta e definição conjunta de prioridades.</p>
          </div>
        </div>
        <div class="s12-step">
          <span class="s12-step-num">2</span>
          <div>
            <h3>Diagnóstico técnico e integração</h3>
            <p>Mapeamento DICOM e integração PACS/RIS com a estrutura do hospital.</p>
          </div>
        </div>
        <div class="s12-step">
          <span class="s12-step-num">3</span>
          <div>
            <h3>Operação assistida</h3>
            <p>Primeiros laudos com acompanhamento diário da nossa equipe.</p>
          </div>
        </div>
      </div>

      <aside class="s12-contact">
        <div class="s12-contact-label">Fale com a PRN</div>
        <div class="s12-contact-row">
          <span class="s12-contact-ico"><svg viewBox="0 0 24 24"><path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/></svg></span>
          <span>(55) 99112-0419 (WhatsApp)</span>
        </div>
        <div class="s12-contact-row">
          <span class="s12-contact-ico"><svg viewBox="0 0 24 24"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg></span>
          <span>(47) 99137-8313</span>
        </div>
        <div class="s12-contact-row s12-destaque">
          <span class="s12-contact-ico"><svg viewBox="0 0 24 24"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg></span>
          <span>contato@prndiagnosticos.com.br</span>
        </div>
        <div class="s12-contact-row">
          <span class="s12-contact-ico"><svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg></span>
          <span>www.prndiagnosticos.com.br</span>
        </div>
        <div class="s12-contact-row">
          <span class="s12-contact-ico"><svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="4"/><path d="M16 8v5a3 3 0 0 0 6 0v-1a10 10 0 1 0-3.92 7.94"/></svg></span>
          <span>@prndiagnosticos</span>
        </div>
      </aside>
    </div>

    <div class="s12-sign"><strong>Paulo Rogerio Novack</strong> — Representante Legal · PRN Serviços de Radiologia Ltda.</div>
  </div>
</section>

<style>
/* ===== SLIDE 13 — CONTRACAPA ===== */
.s13-wrap {
  position: relative;
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 64px;
}
/* ---- Logo com halo/glow radial (sem placa) — print-safe ---- */
.s13-logo {
  position: relative;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}
.s13-logo__halo {
  position: absolute;
  left: 50%; top: 50%;
  width: 1240px; height: 720px;
  transform: translate(-50%, -50%);
  pointer-events: none;
  background:
    radial-gradient(500px 208px at 50% 49%,
      rgba(255,255,255,0.90) 0%,
      rgba(255,255,255,0.85) 30%,
      rgba(250,251,255,0.48) 47%,
      rgba(235,237,250,0.18) 64%,
      rgba(235,237,250,0.00) 83%),
    radial-gradient(760px 300px at 50% 51%,
      rgba(214,220,244,0.17) 0%,
      rgba(214,220,244,0.055) 47%,
      rgba(214,220,244,0.00) 75%);
  filter: blur(15px);
}
.s13-logo img { position: relative; width: 340px; height: auto; display: block; }
.s13-tagline {
  margin-top: 42px;
  font-size: 18px;
  font-weight: var(--fw-regular);
  letter-spacing: 2px;
  line-height: 1.6;
  color: var(--lavanda);
}
.s13-site {
  position: absolute;
  left: 0; right: 0;
  bottom: 58px;
  text-align: center;
  font-size: 19px;
  font-weight: var(--fw-semibold);
  letter-spacing: 1px;
  color: var(--branco);
}
</style>
<section class="page page-escuro" id="page-13">
  <div class="bleed">
    <div class="blob" style="width: 560px; height: 470px; right: -170px; bottom: -200px; opacity: 0.06;"></div>
  </div>

  <div class="s13-wrap">
    <div class="s13-logo">
      <span class="s13-logo__halo"></span>
      <img src="assets/logo.png" alt="PRN Diagnósticos">
    </div>
    <p class="s13-tagline">Diagnóstico por imagem em que os médicos confiam, desde 2007.</p>
  </div>

  <div class="s13-site">www.prndiagnosticos.com.br</div>
</section>
