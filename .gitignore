# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dev-dist
dev-dist-ssr
*.local
.pnpm-store
.pnpm

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Evaluation screenshots
screenshot.png
.eslintcache
.oxlintcache

# Environment variables
.env*
!.env.example

# Certificado de assinatura de codigo: a chave privada nunca vai para o repositorio.
# Quem tem o .pfx assina qualquer binario como se fosse o Central Whats.
*.pfx
*.p12
*.pvk
*.snk

# Local agent/MCP configuration may contain private API keys.
opencode.json
Whats/

# Linux kernel core dumps (e.g. when oxfmt or pnpm crash)
core.[0-9]*

# Git worktrees (isolated workspaces, e.g. perf/* work)
.worktrees/

# Saida do build do Electron (806 MB de binarios; vai para o GitHub Releases)
central-whats-app/release/
